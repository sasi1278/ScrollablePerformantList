import { StyleSheet } from "react-native";
import { screenHeight,screenWidth } from "../../assets/Dimensions";
const styles=StyleSheet.create({
    container:{
        width:screenWidth*0.95,
        height:screenHeight*0.08,
        marginTop:50,
        marginHorizontal:20,
        borderWidth:1
    }
})

export default styles;